name: Docker Image CI

# DISABLED: No Dockerfile - using Railway for deployment
# on:
#   push:
#     branches: [ "main" ]
#   pull_request:
#     branches: [ "main" ]

jobs:

  build:
    runs-on: ubuntu-latest
    if: false  # Always skip - no Dockerfile
    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: echo "Docker build disabled - using Railway deployment"
