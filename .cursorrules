# 🔒 STRICT PROJECT RULES - MMC MCP BRIDGE

## ALLOWED FILES ONLY (EXACTLY 6):
1. railway.json
2. package.json
3. package-lock.json (OR pnpm-lock.yaml OR yarn.lock - ONLY ONE)
4. README.md
5. .env.example
6. app/page.tsx

## FORBIDDEN FILES (NEVER CREATE THESE):
- NO tsconfig.json
- NO next.config.js or next.config.mjs
- NO .eslintrc.* or .prettierrc.*
- NO additional app/ subdirectories (no app/api/, app/components/, etc.)
- NO src/ folder
- NO components/ folder
- NO lib/ or utils/ folders
- NO test files
- NO Dockerfile
- NO .github/ folder
- NO CI/CD configs
- NO additional markdown files

## RULES FOR AI ASSISTANT:
1. NEVER create files outside the allowed list
2. NEVER suggest creating config files
3. NEVER create helper scripts
4. NEVER create documentation beyond README.md
5. ALL functionality MUST fit in app/page.tsx (UI + API routes combined)
6. If user asks for something requiring forbidden files, ASK FIRST
7. Keep everything RADICALLY MINIMAL
8. Railway deployment via railway.json ONLY
9. No Docker, no CI/CD, no extra tooling

## IF IN DOUBT: DON'T CREATE IT!

This is a RADICAL MINIMALISM project - 6 files, full-stack Next.js app.
